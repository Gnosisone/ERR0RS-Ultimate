# ERR0RS ULTIMATE - Python Dependencies
# Tested: Kali Linux, Parrot OS, Raspberry Pi 5 (Kali ARM), Windows 11

# ── Core Framework ────────────────────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# ── AI / LLM Support ──────────────────────────────────────────────────────────
anthropic>=0.7.8
openai>=1.3.7
# google-generativeai==0.3.0   # optional — uncomment if using Gemini

# ── Database / Cache ──────────────────────────────────────────────────────────
# psycopg2-binary==2.9.9       # optional — PostgreSQL only, skip unless needed
redis==5.0.1

# ── Web / HTTP ────────────────────────────────────────────────────────────────
requests==2.31.0
httpx>=0.25.0
aiofiles==23.2.1
websockets>=12.0
jinja2==3.1.2

# ── Security Tools ────────────────────────────────────────────────────────────
python-nmap==0.7.1

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.0
psutil==5.9.6
rich>=13.0.0
click>=8.0.0

# ── Report Generation ─────────────────────────────────────────────────────────
markdown==3.5.1
reportlab==4.0.7
# weasyprint==60.1             # optional — heavy PDF engine, skip on Pi/VM

# ── UI (Desktop only — skip on headless Pi/VM) ───────────────────────────────
# PyQt5==5.15.10               # optional — GUI only, not needed for CLI/web UI
# pygame==2.5.2                # REMOVED — not used by ERR0RS core

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==7.4.3
pytest-asyncio==0.21.1

# ── Screen info (optional) ────────────────────────────────────────────────────
# screeninfo==0.8.1            # optional — display detection, skip on headless
