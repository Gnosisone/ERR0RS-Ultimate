# UI Archive

Abandoned / superseded UI work, kept for reference but intentionally NOT
served by the launcher (this directory is outside the web root `src/ui/web/`).

## err0rs_2026.html
A 617-line alternate dashboard redesign, last worked on 2026-04-20. It was
never linked from anywhere and was superseded by the current `web/index.html`.
Archived 2026-06-02 rather than deleted in case the redesign is revived.
It makes 2 backend `/api/` calls, so reviving it would need those verified
against the current launcher routes.

## phoenix.html
A 397-line static mascot preview (no backend calls), last touched 2026-04-03.
It was never wired to any function or linked from anywhere and was never in a
working/prompted state. Archived 2026-06-02. A custom phoenix file is planned
as a future replacement, so this is kept for reference rather than deleted.
